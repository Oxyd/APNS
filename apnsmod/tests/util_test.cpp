#include "util.hpp"
#include <gtest/gtest.h>

// This file is empty. It didn't use to be, though. I'll leave it here in case it ever becomes handy later on.

int main(int argc, char** argv) {
  testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}
